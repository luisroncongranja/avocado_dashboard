services:
  - type: web
    name: avocado-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    envVars:
      - key: DASH_DEBUG
        value: "false"
